machine:
  node:
    version: 6.2.2

general:
  branches:
    ignore:
      - gh-pages

dependencies:
  pre:
    - npm install
  cache_directories:
    - node_modules

test:
  override:
    - npm test

deployment:
  master:
    branch: master
    commands:
      - npm run build
      - ./scripts/deploy.sh
